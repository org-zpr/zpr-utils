[package]
name = "cbpf-rs"
version = "0.2.0"
edition = "2021"

[dependencies]
pcap = { version = "2.0.0", optional = true }

[dev-dependencies]
pcap = { version = "2.0.0" }

[features]
pcap = ["dep:pcap"]
